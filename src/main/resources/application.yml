spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:${MYSQL_URL}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
   
 
springdoc:
    api-docs:
        path: /api/open-api
    swagger-ui:
        path: /api/docs
        
       
